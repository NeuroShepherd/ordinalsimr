# .onLoad

This function is called when the package is loaded. It sets the default
options for the package.

## Usage

``` r
.onLoad(libname, pkgname)
```

## Arguments

- libname:

  libname

- pkgname:

  package

## Value

invisible
